@using Sandbox
@using Sandbox.UI

@namespace OITC

@inherits Panel

@code
{
	protected override int BuildHash()
	{
		return System.HashCode.Combine(BBGame.Current.CurrentGameState.Tier);
	}
}
@{
	SetClass( "toggled", BBGame.Current.CurrentGameState.Tier == GameState.MidGame );
}
<root>
	<label>
		@BBGame.Current.CurrentGameState.Text
	</label>
</root>
@using System;
@using Sandbox;
@using Sandbox.UI;

@namespace OITC
@inherits Panel

@code
{
	public IClient Client { get; set; }

	protected override int BuildHash()
	{
		return HashCode
		.Combine(Client.GetInt("kills"), Client.GetInt("deaths"), Client.Name.FastHash());
	}
}

@{
	var color = Color.Random;
	var lightColor = color.WithAlpha(0.25f);
}

<root style="background-image: linear-gradient(to left, @lightColor.WithAlpha(0.1f).Rgba, rgba(0, 0, 0, 0));">
	<div class="pfp" style="background: url( avatar:@Client.SteamId ); border: 1px @color.Rgb"></div>
	<label class="name" style="color:@(lightColor.Rgb)">@(Client.Name)</label>
	<div class="score">
		<div>
			<label style="color: white; font-size: 24px; align-self: center;">K</label>
			<label class="name" style="color:white; font-size: 32px; align-self: center;">@Client.GetInt("kills")</label>
		</div>
		<div style="width: 3%; background-color: white;"></div>
		<div>
			<label style="color:white; font-size: 24px; align-self: center;">D</label>
			<label class="name" style="color:white; font-size: 32px; align-self: center;">@Client.GetInt("deaths")</label>
		</div>
	</div>
</root>
